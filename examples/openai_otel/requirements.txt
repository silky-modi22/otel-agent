# Optional venv: python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
# Pin with root repo OTEL stack (1.x)
openai>=1.40.0,<2
opentelemetry-api>=1.27.0,<2
opentelemetry-sdk>=1.27.0,<2
opentelemetry-exporter-otlp-proto-http>=1.27.0,<2
opentelemetry-instrumentation-openai-v2>=2.0b0,<3
